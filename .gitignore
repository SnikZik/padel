# client source package (kept locally; large media, not needed to serve the site)
handoff/
# generated deliverables
screenshots/
*.zip
.DS_Store

# client briefs: business details, not for the public repo
brief/

# client-supplied source images (originals, not served)
handoff-extra/
